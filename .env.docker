# ── PostgreSQL ──
POSTGRES_USER=test
POSTGRES_PASSWORD=test100
POSTGRES_DB=appointment_db

# ── Backend ──
NODE_ENV=production
PORT=4000
DATABASE_URL=postgresql://test:test100@db:5432/appointment_db
CORS_ORIGIN=http://localhost:3000
API_PREFIX=/api/v1
API_KEY=testapi
JWT_SECRET=AEdFq3CGcoZVnV8GxyJm3YEdejJMD3JCJ/Q65GhlLMM=
JWT_EXPIRES_IN=15m
REFRESH_EXPIRES_IN_DAYS=7

# ── Frontend (build-time) ──
NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1
